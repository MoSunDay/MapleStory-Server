Commit: 5391bb19e837afeb65f332262d0d80b882b0c952

# 普通新角色四维初始化为 20

日期: 2026-08-12

## 变更

- `CharacterFactoryRecipe` 的 STR、DEX、INT、LUK 默认值由 4 统一调整为 20。
- 登录角色创建入口使用的冒险家、骑士团、战神配方均继承该默认值，创建响应与数据库落库保持一致。
- 已有角色不受影响；不修改表结构，不执行数据库数据更新或删除。

## 边界

- 通过特殊道具创建的 30 级职业角色仍可由各职业配方覆盖默认属性，保留其既有职业初始化规则。

## 验证

- 服务端全量 Java 编译通过。
- 独立构造 `CharacterFactoryRecipe` 验证 STR/DEX/INT/LUK 均为 20。
