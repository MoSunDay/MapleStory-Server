[package]
name = "maple-wz"
version = "0.1.0"
edition = "2021"
rust-version = "1.77"

# This crate is not yet a workspace member.
# Enable after `rustup update stable` and uncomment in root Cargo.toml.

[dependencies]
maple-core = { path = "../maple-core" }
# nom = "7"
# byteorder = "1"
# bytes = "1"
